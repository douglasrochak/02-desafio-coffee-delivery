import styled from "styled-components";

export const CoffeeListContainer = styled.div`
  padding: 2rem 0;

  & > div {
    margin-top: 3.375rem;

    display: flex;
    flex-wrap: wrap;
    gap: 2rem;
  }
`;
